#include <SDL2/SDL.h>
#include <string>
#include "Map.h"
#include "Tile.h"

int main (int argc, char* argv[]) {

    int width = 800;
    int height = 800;
    int tileWidth = 10;
    int tileHeight = 10;

    Map gamestate(800,800,10,10);
}

